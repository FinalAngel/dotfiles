#!/usr/bin/env bash
source "$PWD/utils/index"

# get latest repo state
info "Running update scripts"
git pull origin master -q
success "Updated repository"

# run general updates
warn "Homebrew update disabled, re-enable"
# source "$PWD/homebrew/update"
source "$PWD/node/update"
source "$PWD/git/update"

# system specific updates
if is_mac; then
  source "$PWD/macos/update"
fi
