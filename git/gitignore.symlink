# general
*~
*.swp

# editors
.idea
.vscode

# thumbnail cache files
._*
Thumbs.db

# node
node_modules
npm-debug.log
.sass-cache

# python
*.pyc
.python-version

# folder view configuration files
.DS_Store
Desktop.ini

# files that might appear on external disks
.Spotlight-V100
.Trashes
.TemporaryItems
.VolumeIcon.icns
.fseventsd

# work
.aldryn
.divio
data.tar.gz

**/.claude/settings.local.json

# worktrees created by `claude --worktree` / the desktop app, which live inside
# the repository they belong to. `**/` because a pattern with a slash is
# anchored to the repository root otherwise, and this file is a global exclude
**/.claude/worktrees/
